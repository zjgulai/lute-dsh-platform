# 未完成项与残余风险 · 同步处置记录（2026-09-17 00:57）

> 承接 DISPOSITION.md 的交接。本轮只做只读复核 + 落本记录 + 挂收尾监测，
> **未写入任何父会话撞面文件**（`scripts/gate.mjs`、`scripts/gates/*`、
> `docs/*`、`.scratch/review/*`）。

## 一、复核证据（本轮实测，新鲜）

| 检查 | 结果 | 证据 |
|---|---|---|
| 父会话是否仍在写 | **是** | `session-72993437` 的 log：00:55:54 10,856,207 B → 00:56:58 10,947,687 B（+91 KB / 64 s）；`scripts/gate.mjs` mtime 00:54:13 |
| M3 改动是否在位 | **在位** | `gate.mjs:42` import、`:1955-2000` 未覆盖行；`gate-result.mjs:247/268` 两个新函数；`gate-result.test.mjs` +6 条；`gate.test.mjs:187` CLI 用例 |
| ADR 编号是否撞号 | **未撞** | 最大仍是 ADR-0102；`docs/adr/README.md` 的未提交改动是父会话的（mtime 00:21:44，早于交接） |
| P-08 是否已被父会话顺手做掉 | **没有** | `--list` 83 条，`grep -c 'remediation:'` 83 处——全部声明但**零断言**；`--list` 不暴露 remediation |
| 看板是否有 QG 卡 | **没有** | board rev 47 中 `QG-` 0 命中——父会话的卡片跟踪面在 `.scratch/review/`，不在看板 |

## 二、逐项处置

| 项 | 本轮处置 |
|---|---|
| 父会话覆盖风险 | 挂后台监测：其余会话日志安静 ≥5 分钟后退出（4 h 上限）。触发后：重读 gate.mjs 复核 M3 → 实施 P-08 → 复跑 `pnpm run gate` / `gate:full` |
| **P-08**（M2 剩余：注册项必须声明非空 remediation） | 方案已定（见 §三）。**写入等父会话收尾**——任何忠实实现都要动 `gate.mjs`（注册表所在、导入即执行），而它是父会话最热的文件 |
| gate.test.mjs 不在门禁射程 | 维持「单独立卡」建议。卡应进父会话的跟踪面（`.scratch/review/` 工作流），不在旧看板 |
| M5（CALIB 门槛） | 维持原判：需先定义「门槛」的可判定形态，属设计变更。缺原始提案全文，未擅自起草 |
| C2（写入通道 → `~/.dsh/AGENTS.md`） | 维持原判：**不单方面写全局文件**。要做需用户明确授权 + 给草案 |
| M4（装载点副本改符号链接，R2） | 维持原判：需立项。缺原始提案上下文，未擅自起草 ADR |
| C3（重试熔断升级 hard stop，R3） | 维持原判：Own——只出 proposal/runbook，且需原始提案上下文 |

## 三、P-08 实施方案（待父会话收尾后执行）

**机制层（治本，零运行时成本）**：`gate.mjs` 在 `CHECKS` 注册表构造完成后、
`main()` 之前调用断言——第 84 条缺 `remediation` 时**启动即崩红**，不依赖任何
测试去跑它。断言函数放 `scripts/gates/gate-result.mjs`（可导入、已有
`gate-result-selftest` 射程），签名 `assertRemediationDeclared(checks)`：
每个项的 `remediation` 必须是非空字符串，违规抛错并点名 `check.name`。

**测试层（证明函数会说「不」，防 P-02 假绿）**：`gate-result.test.mjs` 加纯函数
用例——缺字段 / 空串 / 空白串 / 非字符串 / 垃圾输入 → 抛且点名；合法注册表 → 不抛。
这些在 `gate-result-selftest` 的**现有射程内**（比 CLI 级便宜一个数量级）。

**已知残留（照实写，不粉饰）**：删掉 `gate.mjs` 里的那一行调用，没有任何门禁
测得到——这正是 §四「`gate.test.mjs` 不在射程内」的同一个洞。所以 P-08 的机制
与 §四 的 CLI 层覆盖缺口是**同一洞的两面**：要么接受「启动断言 + 纯函数用例」
（删调用这个动作靠人/靠 §四 的卡兜底），要么先把 `gate.test.mjs` 接进 full
（代价 ~4.5 min）。二者择一由用户定，不在父会话收尾前动。

## 四、P-08 执行记录（2026-09-17 01:20，父会话 01:09:43 收尾后）

监测任务触发：其余会话日志安静 ≥5 分钟（最后写入 01:09:43，01:15:24 检出）。
复核 M3 仍在位后实施 P-08，**改动面 3 个文件**：

- `scripts/gates/gate-result.mjs`：新增 `assertRemediationDeclared(checks)`——
  纯函数、`{ valid, errors }` 与 `validateGateResult` 同形，垃圾输入判红不抛。
- `scripts/gate.mjs`：import 该函数；`CHECKS` 构造后、`main()` 之前调用，
  违规即 stderr 逐条点名 + `process.exit(1)`（任何模式启动即自检）。
- `scripts/gates/gate-result.test.mjs`：新增 5 条用例（全声明通过 / 缺字段·空串·
  空白串·非字符串判红点名 / 无 name 用序号点名 / 垃圾输入不抛）。

**验证证据（实跑）**：

| 步骤 | 读数 |
|---|---|
| Red | 函数不存在时 `node --test` 整文件失败（fail 1） |
| Green | 实现后 **24/24 pass**（原 19 + 新 5） |
| 基线 | `node scripts/gate.mjs --list` exit=0，86 条（父会话收尾前又注册 3 条，均声明了 remediation） |
| 变异 | 清空 22 条 remediation → exit=**1**，stderr 首行 `gate 注册表缺 remediation：package-identity: remediation must be a non-empty string`，共点名 22 条 |
| 还原 | exit=0、86 条复绿 |
| 门禁 | `pnpm run gate` **76/78、退出码 0**（跳过 2），M3 摘要行正常 |

**未做（维持原判）**：未 commit / push（无授权）；§四 CLI 层覆盖缺口仍开放；
M5 / C2 / M4 / C3 状态不变。
