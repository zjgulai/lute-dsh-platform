# —— LUTE Agentic System · monorepo 白名单式 .gitignore ——
# 策略：默认忽略全部 → 恢复目录 → 仅纳入经审批的目录与根级元文件
# （一次性补丁脚本/预览 HTML/构建产物/临时文件不入库）
# 维护规则：白名单条目必须指向磁盘上真实存在的路径；门禁 verify:* 会拒绝幽灵条目（ADR-0013）
# 2026-09-11：移除 !81-Skills/** —— 该创作源已迁出仓库至 ~/project/81-Skills/，
# 白名单条目随之失效（门禁 gitignore-whitelist 会拒绝幽灵条目）。
# 2026-09-11（G6）：再移除 5 条 —— dsh-renderer-heal / dsh-reverse-skill-local /
# dsh-skill-title-fix / explore-unknowns-local / write-spec-local 已归档出工作树
# （副本在 ~/project/_archive/Magpie-Horch-20260911/）；其文档已入 docs/。
# !dsh-rootoutlet-heal/** 刻意保留：白屏手册 §6.1 把它作为运行时回滚基线。
# 2026-09-11（G5）：移除 !_doc-notes/** —— 该草稿体系已收编（8 篇插件说明入 docs/install-notes/，
# 4 个草稿归档），与 ADR-0009 的文档脊柱（AGENTS.md → architecture.md → notes/）合并。

*
!*/
!.gitignore
!/README.md
!/CHANGELOG.md
!/LICENSE
!AGENTS.md
!/package.json
!.github/**
!docs/**
!doc/**
!.scratch/**
!packaging/**
!packages/**
!scripts/**
!shared/**
!dsh-patches/**
!dsh-rootoutlet-heal/**

# —— vendor 嵌套仓库：源码不入库，仅 pin 文件入库 ——
!vendor/
!vendor/dsh-desktop.pin
vendor/dsh-desktop/
!vendor/dsh-worktable.pin
vendor/dsh-worktable/

# —— 纳入目录内的常见排除 ——
node_modules/
__pycache__/
.doctor-backup/
staging/
dist/
generated/
packages/**/eval/out/
*.tgz
.env
.env.*
credentials*
*.shn#
.DS_Store
*.log

# —— 排除临时/备份文件（覆盖白名单，避免 git add -A 误纳入）——
*.bak-*
*.bak
*.pre-*
*.orig*
dsh-chatui-fix/
dsh-chatui-fix-backup/

# 2.0.0 构建源（app 直补副本 + rc 时代 profile 源；可重建，勿入库）
staging-src/

# 打包流水线在 packaging/ 下解出的 .app 工作副本（549 MB / 20,177 文件；可重建）
packaging/.app-cache/

# —— dsh-patches 归档素材与 release 目录：受管资产，显式纳入以消除 tracked+ignored 漂移（ADR-0013）——
!dsh-patches/archive/**
!release/**
!packages/**/test/**
!packages/**/tsconfig.json

# ADR-0017：从内建运行时 tgz 解出的 DSH 类型来源，可重建，不入库
.dsh-types/
*.tsbuildinfo

# —— 迁移快照：大体积二进制与 .app 不得进对象库（SEC-RT-011 / ADR-0121）——
# 背景：2026-09-18 实测 packaging/backup/pre-2.0.10-migration/ 下的 6.8 GB
# dsh-home-snapshot.tar 曾被 git add，经 codex checkpoint 的 write-tree 静默写入对象库，
# 内含活凭证 .dsh/.credentials.yaml（与磁盘上当时在用那份逐字节相同）。
# 该对象已定点移除，此规则防止复发。射程刻意收窄到体积型二进制：
# 若整目录排除，会让已跟踪的 packaging/backup/README.md 变成 tracked+ignored 漂移（ADR-0013）。
packaging/backup/**/*.tar
packaging/backup/**/*.tar.gz
packaging/backup/**/*.tgz
packaging/backup/**/*.dmg
packaging/backup/**/*.zip
packaging/backup/**/*.pkg
packaging/backup/**/*.app/

# —— .scratch 研究工具的编译产物：与 .swift 源码同名、无扩展名，可重建（swiftc x.swift -o x）——
# 源码 .swift 入库，编译出的 Mach-O 不入库（本文件开头即声明「构建产物/临时文件不入库」）。
# 2026-09-18 实测：scanrow（54 KB Mach-O）在 .scratch 下未被忽略，一次 git add -A 就会进对象库。
# 射程收窄到具体产物名而非「.scratch 下所有无扩展名文件」——后者会误伤将来可能有意的数据文件。
# 新工具编译出产物后请在此追加一行；这是纪律，不是机制，故门禁 gitignore-whitelist 不守它。
.scratch/sidebar-research/scanrow
