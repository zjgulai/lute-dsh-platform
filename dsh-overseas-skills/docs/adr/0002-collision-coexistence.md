# ADR-0002：撞名技能（tdd/to-spec/grill-me）全局新版 + 预设旧版共存

- Context：仓库的 tdd/to-spec/grill-me 与「AI 产品开发工程师」预设内置同名技能（早期同源导入，版本较旧）撞名。
- Decision：全局 `~/.dsh/skills/` 安装 repo 新版；预设目录旧版不动。依赖 DSH 技能层遮蔽语义：预设会话内预设层优先（旧版生效），非预设会话用全局新版。
- Consequences：零破坏、无需回归预设；代价是同一技能两版并存（版本差异在预设内可见，需文档告知）；未来如需统一，单独决策升级预设副本。
