# 2.3.1 的产物已宣告丢失（2026-09-13）
#
# 事实：packaging/release/2.3.1/ 里只剩 manifest.json / SHA256SUMS / VERSION，dmg 不在本机任何
#       位置（Spotlight 全盘查不到、release/.archive 里没有、本机没有 APFS 本地快照可回滚）。
#
# 为什么不能重建：同号不同字节会让「客户手上那版对应哪份源码」无法回答（ADR-0057）。用还留在
#       staging/2.3.1/payload 的那份载荷重制一支 dmg，得到的哈希不是 339cf853…，那不是 2.3.1。
#
# 找回办法：**任何**外部副本都可核验收回——客户手上那份、聊天软件里那份、别的机器上那份：
#       bash packaging/scripts/release-restore.sh 2.3.1 --from <那份 dmg 的路径>
#       哈希与 release/2.3.1.sha256 对上 = 确是我们发的（并会被归档+锁定）；对不上 = 拒收。
#
# 本文件是**事实记录**，不是豁免：它只让门禁把「早已宣告丢失的那一个」与「刚刚又丢了一个」
# 区分开——后者必须红灯。
