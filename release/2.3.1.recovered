# 2.3.1：丢失宣告 → **已找回**（宣告 2026-09-13 15:18；找回同日 18:06）

## 当时的宣告（原文保留，作为教训的一部分）

- 事实：`packaging/release/2.3.1/` 里只剩 `manifest.json` / `SHA256SUMS` / `VERSION`，dmg 不在
  本机任何位置（**Spotlight 全盘查不到**、`release/.archive` 里没有、本机没有 APFS 本地快照可回滚）。
- 为什么不能重建：同号不同字节会让「客户手上那版对应哪份源码」无法回答（ADR-0057）。用还留在
  `staging/2.3.1/payload` 的那份载荷重制一支 dmg，得到的哈希不是 `339cf853…`，那不是 2.3.1。

## 找回（2026-09-13 18:06）

```
bash packaging/scripts/release-restore.sh 2.3.1 --from ~/Downloads/DSH-Desktop-LUTE-2.3.1-mac-arm64.dmg
→ 实际 sha256 = 339cf853db30ffda338e4a7ba3f2fca97b11a7bffd56a1eb959407432780bbc9  （与清单逐位相同）
→ 仓库产物位 + 仓库外归档 两处均已就位并 uchg 锁定
```

那份副本的取证：`hdiutil verify` = VALID；xattr = `com.apple.quarantine: 0082;…;Feishu;`
——即 ADR-0067 §4 说的「**聊天软件里那份**」。也就是说，宣告「本机无任何副本」的时候，副本一直在，
而且在宣告之前就已在（文件 mtime 14:37 < 宣告 15:18）。

## 取证为什么错（这才是要记住的）

判据是「Spotlight 全盘查不到」。**搜索工具的沉默不是缺席的证据**——查不到只说明没搜到，
不说明不存在。而错宣告的代价不是「少一份文件」：它把一个**本可核验收回**的版本，永久钉成了
「不可重建」，并且让 `release-verify.sh` 从此对该版本**短路跳过字节核对**（见下），
于是这个版本即使再次丢失也不会有人报错。

**规则（已写进 SOP §7）：宣告 `.lost` 前必须跑一次文件系统遍历，而不是一次 `mdfind`**，
并把命令与输出写进 `.lost` 的取证段；重点排查 IM 落地目录（`~/Downloads` 正是飞书/微信收文件的
地方）与外接卷。

## 现状

- 本文件是**事实记录，不是豁免**（已终局）。`release-verify.sh` 只 glob `release/*.sha256`，
  不读本文件，故 2.3.1 已回到常规字节核对：产物在、哈希 ok、清单齐全、归档一致、已锁定。
- 教训的机制化：`release-verify.sh` 现在对「字节已在位、却还留着 `.lost`」判红，防止过期豁免静默生效。
